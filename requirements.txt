openai>=1.0.0
anthropic>=0.30.0
python-dotenv>=1.0.0
transformers>=4.44.0   # optional (only if you want Hugging Face)
torch>=2.2.0           # optional; CPU is fine, GPU is faster
requests>=2.31.0       # optional; used for Ollama local model
